/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true, // Recommended for catching errors
    swcMinify: true,       // Helps optimize the build
  };
  
  export default nextConfig;